// Type declarations for React Native environment.
// These types are normally provided by lib.dom.d.ts but we target ES2020 without DOM.

// Web Crypto API types used by our crypto module
declare type BufferSource = ArrayBufferView | ArrayBuffer;
